{
  "name": "github-actions-pr-traceable-to-work",
  "version": "0.0.1",
  "private": true,
  "description": "GitHub Action that requires pull requests to link to an Azure DevOps work item or GitHub issue",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc",
    "format": "biome format --write src test",
    "format-check": "biome format src test",
    "lint": "biome check src test",
    "package": "ncc build lib/main.js --out dist --source-map --license licenses.txt && printf '%s\\n' '{\"type\":\"commonjs\"}' > dist/package.json",
    "test": "npm run build && npm run package && vitest run",
    "all": "npm run build && npm run package",
    "clean": "shx rm -rf lib dist",
    "verify-package": "test -f dist/index.js && test -f dist/package.json",
    "ci": "npm run lint && npm run format-check && npm run test && npm run verify-package"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JoostVoskuil/github-actions-pr-traceable-to-work.git"
  },
  "keywords": [
    "actions",
    "traceability",
    "issues",
    "Azure Boards"
  ],
  "author": "Joost Voskuil",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "@types/node": "^26.5.0",
    "@vercel/ncc": "^0.45.0",
    "shx": "^0.4.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
